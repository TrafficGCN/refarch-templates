id: add roles
author: Default
realm: local_realm
changes:
  - addRole:
      name: ROLE_ADMIN
      description: Administrator role with full system access
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: ROLE_USER
      description: Basic user role with standard access
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: ROLE_EDITOR
      description: Editor role with content management access
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: ROLE_MODERATOR
      description: Moderator role with content moderation access
      clientRole: true
      clientId: local
      composite: false