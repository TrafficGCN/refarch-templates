package de.muenchen.refarch.link;

/**
 * Enum representing the scope of a link.
 * Links can be either internal (within the system) or external (pointing to external resources).
 */
public enum LinkScope {
    INTERNAL,
    EXTERNAL
}
